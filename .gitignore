# Variáveis de ambiente (NUNCA commitar a chave real)
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/

# Virtualenv
venv/
env/
.venv/

# IDEs
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml

# Banco SQLite da fila de triagens
triagens.db
triagens.db-journal
triagens.db-shm
triagens.db-wal

# Relatórios gerados pelo runner do golden set
eval/results_*.json

# Modelo Random Forest treinado (regerar localmente com python data/treinar_rf.py)
# Não é commitado por: (a) compatibilidade de versão do sklearn entre máquinas,
# (b) pickle binário não diff bem no git, (c) reproduzível a partir do CSV.
data/rf_model.pkl
